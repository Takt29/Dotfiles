[core]
	excludesfile = /Users/yoshida_t/.gitignore_global
	quotepath = false
[alias]
	st = status
	sw = switch
	delete-merged-branch = !git branch --merged | grep -vE '^\\*|master$|develop$' | xargs -I % git branch -d %
